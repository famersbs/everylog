import React from 'react'

import CardForm from '../../../../component/cardform'
import spec from './spec'

const Book = (props) => {
    return (
      <CardForm spec={spec} />
    )
}


export default Book

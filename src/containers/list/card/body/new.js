import React from 'react'

import CardForm from '../../../../component/cardform'
import spec from './spec'

const Body = () => {
    return (
      <CardForm spec={spec} />
    )
}

export default Body

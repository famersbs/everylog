import React from 'react'

import CardForm from '../../../../component/cardform'
import spec from './spec'

const Note = () => {
    return (
      <CardForm spec={spec} />
    )
}

export default Note
